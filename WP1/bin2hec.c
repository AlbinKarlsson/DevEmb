void getBinaryNumber(int i, int array[])
{
    int passArray[16];

    printf("\n");

    while (i % 4 != 0)
    {
        array[i];
        array[i] = 0;
        i++;
    }

    for (i = i - 1; i >= 0; i--)
    {
        passArray[i] = array[i];
        printf("%ld", passArray[i]);
    }
}

int from;
int to;

void checkNumbers(passArray, from, to)
{
    if()

}
